<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:label="Looper Player"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:extractNativeLibs="true"
        android:enableOnBackInvokedCallback="true"
        android:requestLegacyExternalStorage="true">

        <!-- Player Widget Providers -->
        <receiver android:name="com.looper.player.PlayerWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.looper.player.ACTION_PLAY_PAUSE" />
                <action android:name="com.looper.player.ACTION_NEXT" />
                <action android:name="com.looper.player.ACTION_PREV" />
                <action android:name="com.looper.player.ACTION_SHUFFLE" />
                <action android:name="com.looper.player.ACTION_REPEAT" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider"
                android:resource="@xml/player_widget_info" />
        </receiver>

        <receiver android:name="com.looper.player.PlayerWidgetProviderSquareArtwork"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.looper.player.ACTION_PLAY_PAUSE" />
                <action android:name="com.looper.player.ACTION_NEXT" />
                <action android:name="com.looper.player.ACTION_PREV" />
                <action android:name="com.looper.player.ACTION_SHUFFLE" />
                <action android:name="com.looper.player.ACTION_REPEAT" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider"
                android:resource="@xml/player_widget_info_square_artwork" />
        </receiver>

        <receiver android:name="com.looper.player.PlayerWidgetProviderSquareProgress"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.looper.player.ACTION_PLAY_PAUSE" />
                <action android:name="com.looper.player.ACTION_NEXT" />
                <action android:name="com.looper.player.ACTION_PREV" />
                <action android:name="com.looper.player.ACTION_SHUFFLE" />
                <action android:name="com.looper.player.ACTION_REPEAT" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider"
                android:resource="@xml/player_widget_info_square_progress" />
        </receiver>

        <receiver android:name="com.looper.player.PlayerWidgetProviderLargeLyrics"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.looper.player.ACTION_PLAY_PAUSE" />
                <action android:name="com.looper.player.ACTION_NEXT" />
                <action android:name="com.looper.player.ACTION_PREV" />
                <action android:name="com.looper.player.ACTION_SHUFFLE" />
                <action android:name="com.looper.player.ACTION_REPEAT" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider"
                android:resource="@xml/player_widget_info_large_lyrics" />
        </receiver>

        <activity
            android:name="com.looper.player.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <service
            android:name="com.alesdrnz.mpv_audio_kit.media_session.MpvMediaSessionService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback"
            tools:node="merge">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </service>

        <service
            android:name="com.looper.player.LooperTaskService"
            android:enabled="true"
            android:exported="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
