output: dist/
publish:
  # You can add publishing options here (e.g. github)
  
releases:
  - name: dev
    package_name: looper_player
    display_name: Looper Player
    executable_name: looper_player
    targets:
      - deb
      - rpm
      - archive
    build_args:
      --release: 

    deb_options:
      # Optional: custom postinst/prerm scripts
      # postinst: linux/deb/postinst
      
    rpm_options:
      # Optional: custom spec template
      # spec_template: linux/rpm/project.spec
